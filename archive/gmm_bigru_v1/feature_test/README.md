# Historical B2 comparator

`gmm_bigru_baseline.py` is the frozen same-configuration K10-F2 BiGRU
comparator used during the feature-selection study. Its original integration
with `feature-test/evaluate_candidates.py` remains available in Git history;
the implementation and target-blind deterministic test are retained here.
